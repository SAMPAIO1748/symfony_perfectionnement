{% extends "admin/base.html.twig" %}

{% block main %}

	{{batman}}

	{% for type in types %}


		<h2>{{ type.name }}</h2>

	{% endfor %}

	<hr>

	{% for gproduct in gproducts.getAll %}
		<h3>{{ gproduct.name }}</h3>
	{% endfor %}

{% endblock %}
